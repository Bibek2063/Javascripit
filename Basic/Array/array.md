#array 
an array is an object type designed for storing data collections.
Key characteristice of js array are:
-Elements: list of a valuse, kn as 
-Ordered: based on index
-Zero indexed: first el is at 0 
-Dynamic size: can grow or sink as elements are added or removed
-Heterougous: can store elements of different datatypes(numbers,strings, objects and other types)

#creating an array 
syntax:
 const array_name=[item1,item2,....];
 eg:
 const cars["BMW","Audi","RR"];
 ##you can also create an empty and provide elements late
 ##using js keyword new(but not effficient )
